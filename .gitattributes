# Force Ansible YAML to be counted as YAML
*.yml linguist-language=YAML
*.yaml linguist-language=YAML

# --- Terraform should be HCL (usually auto-detected, but force it) ---
*.tf linguist-language=HCL
*.tfvars linguist-language=HCL

# --- Shell scripts ---
*.sh linguist-language=Shell

# --- Optional: if you have Nginx configs and want them counted as "Nginx" ---
nginx.conf linguist-language=Nginx
*.nginx linguist-language=Nginx

# --- Optional: count env/service files as INI (if you want) ---
*.env linguist-language=INI
*.service linguist-language=INI
